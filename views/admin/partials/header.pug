header.header
    .inner-logo
        a(href=`/${pathAdmin}/dashboard`) 
            span 28
            span Admin
    .inner-wrap
        .inner-notify 
            img(src="/admin/assets/images/chuong.svg", alt="icon") 
            span 6
        .inner-account 
            .inner-avatar 
                img(src=account.avatar, alt="logo")
            .inner-text-account 
                .inner-name #{account.fullName}
                .inner-role #{account.role}
        button.inner-button-menu 
           <i class="fa-solid fa-bars"></i> 